#5.30.17

#pandas variables
passnum = []
posdist = []
posspeed = []
shotmade = []

#pandas for machine learning - setting up data frame
for x in passperpos:
    passnum.append(passperpos[1])

df = pd.DataFrame({ "Avg Speed" : posspeed,
                    "Avg Distance" : posdist,
                    "# of Passes" : passnum
                   })
                   
rows=[]
for num in range(1,total + 1):
    rows.append(num)

df1 = pd.DataFrame(np.nan, index=rows, columns=['Number of Passes', 'Speed (mph)', 'Distance (ft)'])
                

print(df)
print(df1)
